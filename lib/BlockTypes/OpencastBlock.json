{
    "title": "Payload schema of OpencastBlock",
    "type": "object",
    "properties": {
        "series_id" : {
            "type": "string"
        },
        "episode_id": {
            "type": "string"
        },
        "url": {
            "type": "string"
        }
    },
    "required": ["series_id", "episode_id", "url"],
    "additionalProperties": false
}
